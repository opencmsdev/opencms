{
  "name": "opencms",
  "version": "0.3.0",
  "description": "OpenCMS CLI. `opencms init` walks you through choosing a stack; `opencms setup` provisions each integration from opencms.config.ts.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/opencmsdev/opencms.git",
    "directory": "packages/cli"
  },
  "type": "module",
  "bin": {
    "opencms": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=18.17"
  },
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target node",
    "prepublishOnly": "bun run build"
  },
  "dependencies": {
    "aws4fetch": "^1.0.20"
  }
}
