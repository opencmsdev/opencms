/**
 * Public install strings. Unscoped `opencms` is blocked on npm (too similar
 * to `open-cms`); the CLI ships as `@opencms/cli` with bin name `opencms`.
 */
export const NPM_CLI = "@opencms/cli";
export const BUNX_INIT = "bunx @opencms/cli init";
export const BUNX_SETUP = "bunx @opencms/cli setup";
